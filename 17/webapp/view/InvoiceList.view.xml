<mvc:View
	controllerName=""
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m">
    
    <List
        headerText="{i18n>invoiceListTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{invoice>/Invoices}">
        <items>
            <ObjectListItem title="{invoice>Quantity} × {invoice>ProductName}"></ObjectListItem>
        </items>
    </List>

    <FormattedText 
        htmlText="{invoice>/testData/name}"
    />
    
</mvc:View>